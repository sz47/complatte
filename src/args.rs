struct Args {
    input:  String,
    output: String,
    db:     String,
    create: bool
}

pub fn parse_args() {
    let args: Vec<String> = env::args().collect();

    let mut i: i64 = 0;
    for n in args.iter() {
        i += 1;



        
    }
}
